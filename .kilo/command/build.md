---
description: Build the C# project and report errors
agent: debug
---
Build the Godot C# project and report any compilation errors.

1. Run `dotnet build "Claude Game.sln" --verbosity minimal`
2. If there are errors, analyze each one and suggest fixes
3. If the build succeeds, confirm it

Work through errors one by one if there are multiple.
